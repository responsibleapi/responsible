{
  "name": "astro",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "astro": "astro",
    "bld": "astro build",
    "chk": "bun test && bun lint && astro check",
    "clean": "rm -rf dist/",
    "deploy": "bun chk && bun clean && bun bld && bun dpl",
    "dev": "astro dev",
    "dpl": "wrangler pages deploy dist/ --commit-dirty=true",
    "lint": "eslint src/ --fix",
    "preview": "astro preview",
    "start": "astro dev"
  },
  "dependencies": {
    "ace-builds": "^1.24.2",
    "kdljs": "^0.1.5",
    "solid-js": "^1.7.11"
  },
  "devDependencies": {
    "@astrojs/check": "^0.2.0",
    "@astrojs/solid-js": "^3.0.1",
    "@astrojs/tailwind": "^5.0.0",
    "@tailwindcss/typography": "^0.5.10",
    "astro": "^3.0.12",
    "autoprefixer": "^10.4.15",
    "postcss": "^8.4.29",
    "tailwindcss": "^3.3.3",
    "typescript": "^5.0.2",
    "wrangler": "^3.7.0"
  }
}
