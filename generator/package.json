{
  "name": "@responsible/generator",
  "version": "0.0.1",
  "license": "MIT",
  "sideEffects": false,
  "main": "dist/index.js",
  "module": "dist/esm/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "yarn build:cjs && yarn build:esm",
    "build:cjs": "tsc --declaration --module commonjs --outDir dist/",
    "build:esm": "tsc --module es2015 --outDir dist/esm/",
    "lint": "eslint src/ --fix",
    "test": "vitest run src/",
    "watch:test": "vitest"
  },
  "dependencies": {
    "deepmerge-ts": "^5.0.0",
    "kdljs": "^0.1.5"
  },
  "devDependencies": {
    "openapi-schema-validator": "^12.0.2"
  }
}
