/**
 * Notes about selected package.json fields.
 * This file is documentation only, not a manifest.
 */
{
  "scripts": {
    "test": "bun test is faster than vitest"
  },
  "devDependencies": {
    "@types/bun": "zed can't run bun test without this",
    "vitest": "webstorm still doesn't support bun:test",
    "openapi-format": "migrating OpenAPI 3.0.x to 3.1.x"
  }
}
