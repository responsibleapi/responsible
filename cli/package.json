{
  "name": "@responsible/cli",
  "version": "0.2.1",
  "license": "MIT",
  "bin": "dist/index.js",
  "scripts": {
    "bld": "bun bundle.ts",
    "lint": "eslint src/ --fix",
    "start": "bun bld && bun dist/index.js ../examples/listenbox.kdl | pbcopy",
    "start:watch": "bun bld && bun dist/index.js -w -o dist/openapi.json ../examples/listenbox.kdl",
    "tst": "bun test",
    "preversion": "bun test",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "arg": "^5.0.2",
    "chokidar": "^3.5.3"
  },
  "devDependencies": {
    "bun-types": "^0.6.7"
  }
}
